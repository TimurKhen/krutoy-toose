import { ClickEffect } from './click-effect';

describe('ClickEffect', () => {
  it('should create an instance', () => {
    const directive = new ClickEffect();
    expect(directive).toBeTruthy();
  });
});
