import { ClickReactionAnimation } from './click-reaction-animation';

describe('ClickReactionAnimation', () => {
  it('should create an instance', () => {
    const directive = new ClickReactionAnimation();
    expect(directive).toBeTruthy();
  });
});
